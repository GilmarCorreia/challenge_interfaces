# challenge_interfaces

ROS 2 custom message package for the tracking challenge.

Includes the message definitions used by `cam_pub` and `object_tracker`:
- `CamStatus.msg`: camera operational status.
- `ObjectState.msg`: visibility and coordinates of the detected object.

This package provides shared interfaces required for communication between components.
